var C = require('../world.js');
var addRect = require('../addRect.js');

module.exports = {
    // Initialize world here(add platforms)
    initWorld: function (world) {
    },
    // Update world here(move platforms, change size)
    updateWorld: function (world, io) {
    },
    // Destroy world
    destroyWorld: function (world) {
    },
    // When new user is added to the world
    onBorn: function (userList, userCount) {
    },
    // When user is removed from the world(disconnection, die)
    onDie: function (userList, userCount) {
    },
    // Determine spawn position for characters
    spawn: function () {
    }
};